# Network-File-Sharing-Server-and-Client
A C++ client-server file sharing application for a capstone project. This project uses TCP/IP sockets for networking and features a multi-threaded server to handle concurrent clients. Key features include user authentication, file listing, uploading, and downloading, with a simple XOR encryption layer.
